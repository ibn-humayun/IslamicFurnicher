{
  "name": "islamic-furniture",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.tsx",
    "build": "mkdir -p dist/static && (cp -r src/public/* dist/static/ || true) && esbuild src/index.tsx --bundle --minify --platform=neutral --outfile=dist/_worker.js"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3"
  }
}
