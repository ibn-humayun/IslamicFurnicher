{
  "name": "islamic-furniture",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/local.ts",
    "build": "chmod +x download_images.sh && ./download_images.sh && mkdir -p dist && cp -r src/public/* dist/ && esbuild src/index.tsx --bundle --minify --platform=browser --format=esm --outfile=dist/_worker.js"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@hono/node-server": "^1.8.0",
    "@types/node": "^20.11.0",
    "esbuild": "^0.20.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.0.0"
  }
}
